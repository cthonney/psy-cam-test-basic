<%# <script crossorigin src="https://unpkg.com/@daily-co/daily-js"></script>
<script>
callFrame = window.DailyIframe.createFrame();
callFrame.join({ url: 'https://experiment.daily.co/68DzVoZCcrWEerSBmSK9' })
</script> %>

<%# This is the title, responsive for small screens and bigger screens %>

<h1 class= "h3 ml-3 my-5 d-block d-md-none" > Select a Psy to  <br> start your call </h1>
<h1 class= "h3 ml-3 my-5 d-md-block d-none" > Select a Psy to start your call </h1>


  <ul>
    <% @consultations.each do |consultation| %>
      <li>
        <%= link_to consultation.psy, sessions_show_path(consultation: consultation), method: :post, class: "h5" %>
        <p class="text-warning mb-1"> Mentor: <%= consultation.specialties %> </p>
      </li>
    <% end %>
  </ul>


<% if @clicked %>
<script crossorigin src="https://unpkg.com/@daily-co/daily-js"></script>
<script>
if (window.innerWidth <= 600) {
  callFrame = window.DailyIframe.createFrame({
  showLeaveButton: true,
      iframeStyle: {
      position: 'fixed',
      bottom: 0,
      width: '100%',
      height: '100%'

      }
});

  callFrame.join({ url: 'https://experiment.daily.co/68DzVoZCcrWEerSBmSK9' })
} else {
      callFrame = window.DailyIframe.createFrame({
      showLeaveButton: true,
      iframeStyle: {
      position: 'fixed',
      top: 0,
      right: 0,
      width: '50%',
      height: '100%'
      }
});

  callFrame.join({ url: 'https://experiment.daily.co/68DzVoZCcrWEerSBmSK9' })
}

</script>

<% end %>
